[project]
name = "wallet-activity-flows"
version = "0.1.0"
description = "15d wallet activity transfers ingest (staging INSERT-only)"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28",
    "psycopg[binary]>=3.2",
]

[dependency-groups]
dev = []
