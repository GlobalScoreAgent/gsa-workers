[project]
name = "token-prices-import"
version = "0.1.0"
description = "Enrich unpriced positions via DexScreener/CoinGecko into wallets.token_prices"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28",
    "psycopg[binary]>=3.2",
]

[dependency-groups]
dev = []
