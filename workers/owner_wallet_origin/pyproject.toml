[project]
name = "owner-wallet-origin"
version = "0.1.0"
description = "One-shot owner wallet origin/activation import across 8 EVM chains for GSA"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28",
    "psycopg[binary]>=3.2",
]

[dependency-groups]
dev = []
