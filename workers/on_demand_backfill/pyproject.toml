[project]
name = "on-demand-backfill"
version = "0.1.0"
description = "Orchestrator: Ethos history/scores + ERC-8183 satellites (+ stubs)"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28",
    "psycopg[binary]>=3.2",
]

[dependency-groups]
dev = []
