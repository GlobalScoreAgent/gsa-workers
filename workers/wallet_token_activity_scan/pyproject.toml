[project]
name = "wallet-token-activity-scan"
version = "0.1.0"
description = "Scan ERC-20/721 Transfer activity via public eth_getLogs (incremental, per-chain shards)"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28",
    "psycopg[binary]>=3.2",
]

[dependency-groups]
dev = []
