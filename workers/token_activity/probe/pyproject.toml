[project]
name = "token-activity-probe"
version = "0.1.0"
description = "Token activity probe: ERC-20/721 Transfer via public eth_getLogs (incremental, per-chain shards)"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28",
    "psycopg[binary]>=3.2",
]

[dependency-groups]
dev = []
