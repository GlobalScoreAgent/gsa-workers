[project]
name = "wallet-holdings-discovery"
version = "0.1.0"
description = "Unified token contracts + portfolio + LP discovery with Alchemy 429 backoff"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28",
    "psycopg[binary]>=3.2",
    "eth-abi>=5.0",
    "eth-utils>=5.0",
    "pycryptodome>=3.20",
]

[dependency-groups]
dev = []
