[project]
name = "dune-queries-import"
version = "0.1.0"
description = "Import Dune reference queries (CEX, mixers, bridges, OFAC) into wallets schema for GSA"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28",
    "psycopg[binary]>=3.2",
]

[dependency-groups]
dev = []
