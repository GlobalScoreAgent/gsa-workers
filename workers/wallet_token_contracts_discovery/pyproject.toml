[project]
name = "wallet-token-contracts-discovery"
version = "0.1.0"
description = "Discover ERC-20 contracts with balance > 0 per wallet+chain via Alchemy"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28",
    "psycopg[binary]>=3.2",
]

[dependency-groups]
dev = []
