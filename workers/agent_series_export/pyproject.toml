[project]
name = "agent-series-export"
version = "0.1.0"
description = "Arbol de series de 30 dias por agente → bucket publico Supabase Storage agent-series"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28",
    "psycopg[binary]>=3.2",
]

[dependency-groups]
dev = []
