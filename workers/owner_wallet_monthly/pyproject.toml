[project]
name = "owner-wallet-monthly"
version = "0.1.0"
description = "Monthly owner wallet balance/nonce and origin history across 8 EVM chains for GSA"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28",
    "psycopg[binary]>=3.2",
]

[dependency-groups]
dev = []
