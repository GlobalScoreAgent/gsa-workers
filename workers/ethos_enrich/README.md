# ethos_enrich (deprecated)

**Absorbido por** [`on_demand_backfill`](../on_demand_backfill/README.md) (steps `ethos_history` + `ethos_scores`).

No uses este directorio para runs nuevos. El workflow `ethos-enrich.yml` está retirado; ver [docs/DEPRECATION.md](../../docs/DEPRECATION.md).

Código histórico puede permanecer aquí como referencia; la implementación viva está en `workers/on_demand_backfill/src/ethos/` y `src/steps/ethos_*.py`.
