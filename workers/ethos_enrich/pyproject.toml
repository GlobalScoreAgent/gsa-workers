[project]
name = "ethos-enrich"
version = "0.1.0"
description = "Ethos history fetch (Goldsky) + official credibility scores (API v2)"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28",
    "psycopg[binary]>=3.2",
]

[dependency-groups]
dev = []
